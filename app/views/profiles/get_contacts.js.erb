$('.contact_selection').empty();
$('.contact_selection').append( $('<option>Escolha um instrumento</option>'));

<% @contacts.each do |contact| %>
  $('.contact_selection').append($('<option value="<%= contact.id %>"><%= contact.name %></option>'));
<% end %>